# 제보 전화 — 라운드 도중 수배 공개 구현 플랜 (#102)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 라운드 시작에 예비 용의자 풀을 전부 확정해 두고 앞 N명만 수배로 공개한다. 본부의 제보 전화를 받으면 대기 중이던 용의자 1명이 수배 리스트에 추가된다.

**Architecture:** 기존 배정 파이프라인(`CriminalAssigner` → `AppearanceAssigner` → `WantedListManager`)의 **마지막 발행 지점만** 공개분/대기분으로 가른다. 선정·외형·디코이·몽타주 생성은 전부 그대로 라운드 시작에 끝난다. 승격은 `OnMontageGenerated` 재발행이므로 `WantedListManager`는 **한 줄도 고치지 않는다** — `NetworkList` 추가와 `TotalWanted++`를 이미 그 핸들러에서 한다.

**Tech Stack:** Unity 6000.3.15f1 / Netcode for GameObjects / C#

**설계 근거:** [docs/superpowers/specs/2026-07-27-tip-call-design.md](../specs/2026-07-27-tip-call-design.md)

## Global Constraints

모든 태스크에 암묵적으로 적용된다.

- **서버 권위 + 오프라인 폴백** — 수신 타이머·승격은 서버(또는 오프라인) 전용. 권위 판정은 `private bool IsAuthority => !IsSpawned || IsServer;`로 통일한다 (`SuddenEventManager`·`BombDevice`와 동일). 동기화 값 읽기는 `IsSpawned && !IsServer ? m_syncedX.Value : m_x` 이중 구조.
- **클라 게이팅은 피드백용일 뿐** — `CanInteract`로 막은 것은 서버 RPC 핸들러에서 **반드시 한 번 더** 막는다 (`CCTVSwitcher.RequestTogglePowerRpc` 선례, #362).
- **네이밍 (GDD 10-5)** — `m_`(private/protected 인스턴스), `s_`(static), `k_`(const), `I` 접두사(인터페이스), camelCase(매개변수/지역변수), PascalCase(public 프로퍼티/메서드/enum 값), `On` 접두사(이벤트).
- **튜닝값은 인스펙터로** — 울림 지속시간·수신 간격·최대 수배 수·최초 공개 수는 하드코딩하지 않는다 (설계 §1).
- **주석은 "왜"를 적는다** — 이 저장소의 기존 주석 밀도와 톤을 따르고 이슈 번호(`#102`)를 남긴다.
- **한 태스크 = 한 커밋.** 커밋 메시지 끝에 `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

### 설계 문서와 코드가 어긋난 지점 (이 플랜에서 정정)

설계 §3의 가드 표는 실제 API와 두 군데 다르다. **코드 기준을 따른다.**

| 설계 문서 표기 | 실제 API | 비고 |
|---|---|---|
| `ArrestJudge.JudgedNpcs` | `NpcController.IsDelivered` | 판정 완료 표식은 NPC가 들고 있다 (#230). `ArrestJudge`에 그런 컬렉션은 없다 |
| "연행 중인 NPC (`PlayerEscorter`)" | `PlayerEscorter.FindEscorterOf(npc) != null` | 연행과 밧줄 끌기를 **둘 다** 본다 (#269) — 끌기만으로도 곧 인계된다 |

또한 설계 §3 마지막 문단("후보를 다 훑어도 유효한 대상이 없으면 그 시점부터 풀 소진으로 본다")은 **연행 중 일시 상태를 소진으로 오인한다.** 마지막 예비 용의자를 끌고 가는 중에 전화가 울리면 그 라운드 전화가 영영 끊긴다. 그래서 두 질의를 분리한다 (Task 4):

- `HasPendingSuspect` — **남아 있는가.** 연행·끌기 중도 "남아 있다"로 센다(판정되면 `IsDelivered`로 자동으로 빠지고, 석방되면 다시 후보). 전화를 계속 걸지의 기준.
- `PromoteNext()` — **지금 승격 가능한가.** 연행·끌기 중을 건너뛴다. 실패는 그 기회 한 번을 놓친 것일 뿐, 정지 사유가 아니다.

### 검증 방식에 대한 주의

**이 저장소에는 자동화 테스트가 없다** (CLAUDE.md). 각 태스크 검증은 둘로 한다:

1. **컴파일 확인** — Unity Editor로 포커스를 옮겨 컴파일이 끝나기를 기다린 뒤 Console에 에러가 없는지 본다. (MCP 연결 시: `refresh_unity` → `read_console(types:["error"])`)
2. **Play 모드 수동 검증** — 태스크마다 명시된 절차를 따른다.

새 타입·멤버를 쓰기 전에 반드시 컴파일이 통과했는지 확인할 것.

---

## File Structure

| 파일 | 책임 | 태스크 |
|---|---|---|
| `Assets/Scripts/NPC/CitizenIdentity.cs` | `SetCriminal(bool)` — 범인 여부만 떼어낸 진입점 | 1 |
| `Assets/Scripts/NPC/AppearanceAssigner.cs` | 몽타주 발행을 공개분만으로 게이트 + `RevealMontage()` | 2 |
| `Assets/Scripts/NPC/CriminalAssigner.cs` | 예비 풀 확정 / 공개 분리 | 3 |
| `Assets/Scripts/NPC/CriminalAssigner.cs` | `PromoteNext()` · `HasPendingSuspect` + 승격 가드 | 4 |
| `Assets/Scripts/HQ/TipCallPhone.cs` | **신규** — 서버 수신 타이머 + `IInteractable` | 5 |
| `Assets/Scripts/Test/TestCriminalLabel.cs` | 공개/미공개 라벨 구분 (테스트 판별용) | 6 |
| `docs/GDD.md` | 6장 178줄 "(제안)" 항목을 확정으로 | 6 |

**손대지 않는 파일:** `WantedListManager.cs`(승격이 `OnMontageGenerated`를 재사용하므로 변경 불필요), `ArrestJudge.cs`(미공개 = `IsCriminal false` = 오검거가 이미 맞는 판정), `JailZone.cs`(정산 기준이 `IsCriminal`이라 승격 후 검거는 자동으로 진범 정산).

---

## Task 1: CitizenIdentity에 SetCriminal 추가

지금 범인 여부는 `AssignProfile(profile, isCriminal)`로만 정할 수 있어 프로필 재배정 없이는 못 바꾼다. 승격은 프로필을 건드리면 안 되므로(동기화 스냅샷이 다시 나간다) 범인 여부만 여는 진입점을 뗀다. **순수 추가**라 기존 동작을 바꾸지 않는다.

**Files:**
- Modify: `Assets/Scripts/NPC/CitizenIdentity.cs`

**Interfaces:**
- Produces: `CitizenIdentity.SetCriminal(bool)` → `void` — Task 4가 호출한다.

- [ ] **Step 1: SetCriminal 추가**

`Assets/Scripts/NPC/CitizenIdentity.cs`의 `AssignProfile` 메서드 **바로 아래**, `AssignAppearance` XML 주석 **바로 위**에 삽입한다:

```csharp

    /// <summary>
    /// 범인 여부만 바꾼다 — 프로필은 그대로 둔다. 제보 전화 승격(#102) 전용.
    /// 프로필까지 다시 배정하면 CitizenData 동기화 스냅샷이 재전송되어, 본부가 보고 있던
    /// 스캔 표시값이 이유 없이 깜빡인다. IsCriminal은 서버 전용이라 동기화할 것이 없다.
    /// </summary>
    public void SetCriminal(bool isCriminal)
    {
        IsCriminal = isCriminal;
    }
```

또한 `IsCriminal` 프로퍼티의 XML 주석을 교체한다. 기존:

```csharp
    /// <summary>실제 범인 여부 — 진범 판정(#41)의 정답 기준. 서버 전용 (클라이언트에서는 항상 false).</summary>
```

교체 후:

```csharp
    /// <summary>
    /// 실제 범인 여부 — 진범 판정(#41)의 정답 기준. 서버 전용 (클라이언트에서는 항상 false).
    /// 곧 공개 플래그이기도 하다 (#102): 라운드 시작에 확정된 예비 용의자는 false로 대기하다가
    /// 제보 전화 승격 시 켜진다. 대기 중에 잡으면 오검거로 판정된다 (GDD 7-3과 일치).
    /// </summary>
```

- [ ] **Step 2: 컴파일 확인**

Unity Editor로 포커스를 옮기고 컴파일이 끝나기를 기다린다.
기대: Console에 에러 0건.

- [ ] **Step 3: 커밋**

```bash
git add Assets/Scripts/NPC/CitizenIdentity.cs && git commit -m "제보 전화 — 범인 여부만 여는 SetCriminal 진입점 (#102)"
```

---

## Task 2: 몽타주 발행을 공개분만으로 가르고 RevealMontage 추가

발행 지점을 `IsCriminal`로 게이트한다. **이 시점에는 아직 예비 용의자가 없어(전원 `IsCriminal = true`) 동작이 바뀌지 않는다** — Task 3에서 처음 관측된다. 순서를 이렇게 잡는 이유: 반대로 하면 Task 3 직후 예비 용의자까지 수배 리스트에 전부 뜨는 깨진 중간 상태가 생긴다.

`m_montageTexts`는 예비분까지 전원 보관한다 — 승격 때 **그대로** 다시 쓴다. 재생성하면 공개 축을 다시 뽑아야 하고, 그러면 본부에 떠 있던 기존 몽타주가 무효가 된다 (설계 §2).

**Files:**
- Modify: `Assets/Scripts/NPC/AppearanceAssigner.cs`

**Interfaces:**
- Consumes: `CitizenIdentity.IsCriminal`
- Produces: `AppearanceAssigner.RevealMontage(NpcController)` → `void` — Task 4가 호출한다.

- [ ] **Step 1: AssignAll의 4번 블록을 발행 게이트로 교체**

`AssignAll` 안의 `// 4. 범인별 몽타주 텍스트` 블록 전체(`for` 루프 + 바로 아래 `Debug.Log` 줄)를 다음으로 교체한다:

```csharp
        // 4. 범인별 몽타주 텍스트 — 예비 용의자분까지 전원 생성해 보관한다 (#102).
        //    보관해 두는 이유: 승격 시 RevealMontage가 이 텍스트를 그대로 쓴다. 그 시점에 재생성하면
        //    공개 축을 다시 뽑아야 하고, 본부에 이미 떠 있던 몽타주가 무효가 된다.
        int revealedCount = 0;
        for (int i = 0; i < criminals.Count; i++)
        {
            AppearanceProfile p = m_criminalProfiles[i];
            string montageText = m_appearanceDatabase.BuildMontageText(p, m_revealedAxes);
            m_montageTexts.Add(montageText);

            // 발행은 이미 공개된 수배만 — 예비 용의자는 IsCriminal = false로 대기하다가
            // 제보 전화 승격 시 RevealMontage로 같은 텍스트가 나간다 (#102)
            CitizenIdentity identity = criminals[i].GetComponent<CitizenIdentity>();
            if (identity == null || !identity.IsCriminal)
                continue;

            revealedCount++;
            OnMontageGenerated?.Invoke(criminals[i], montageText);
        }
        Debug.Log($"외형 배정 완료 ({npcs.Count}명, 용의자 {criminals.Count}명 중 공개 {revealedCount}명) | 몽타주: {string.Join(" / ", m_montageTexts.ConvertAll(t => $"\"{t}\""))}\n{logBuilder}");
```

- [ ] **Step 2: RevealMontage 추가**

`AssignAll` 메서드 **바로 아래**, `RealizeCriminal`의 XML 주석 **바로 위**에 삽입한다:

```csharp

    /// <summary>
    /// 대기 중이던 용의자의 몽타주를 지금 발행한다 — 제보 전화 승격(#102) 전용. 서버(또는 오프라인) 전용.
    /// 라운드 시작에 보관해 둔 텍스트를 그대로 쓰므로 공개 축은 바뀌지 않는다.
    /// 발행 자체는 라운드 시작 때와 같은 경로(OnMontageGenerated)라, WantedListManager의
    /// NetworkList 추가와 TotalWanted++ 가 그대로 따라온다 — 별도 등록 경로를 만들지 않는 이유다.
    /// </summary>
    public void RevealMontage(NpcController npc)
    {
        if (npc == null)
            return;

        IReadOnlyList<NpcController> criminals = Assigner != null ? Assigner.CriminalNpcs : null;
        if (criminals == null)
        {
            Debug.LogWarning("AppearanceAssigner: CriminalAssigner를 찾지 못해 몽타주를 공개할 수 없다", this);
            return;
        }

        // CriminalNpcs와 m_montageTexts는 같은 순서다 — 인덱스로 짝을 찾는다
        for (int i = 0; i < criminals.Count; i++)
        {
            if (criminals[i] != npc)
                continue;

            if (i >= m_montageTexts.Count)
            {
                Debug.LogWarning($"AppearanceAssigner: {npc.name}의 몽타주가 아직 생성되지 않아 공개할 수 없다", npc);
                return;
            }

            OnMontageGenerated?.Invoke(npc, m_montageTexts[i]);
            return;
        }

        Debug.LogWarning($"AppearanceAssigner: {npc.name}은(는) 용의자 목록에 없어 공개 대상이 아니다", npc);
    }
```

- [ ] **Step 3: 클래스 XML 주석 갱신**

클래스 주석의 다음 줄을

```csharp
/// 진범이 여러 명이면(#127) 몽타주도 범인 수만큼 생성되고, OnMontageGenerated가 범인마다 발행된다.
```

교체한다:

```csharp
/// 진범이 여러 명이면(#127) 몽타주도 범인 수만큼 생성된다. 다만 발행(OnMontageGenerated)은
/// 이미 공개된 수배만 — 대기 중인 예비 용의자(#102)는 RevealMontage로 나중에 발행된다.
```

- [ ] **Step 4: 컴파일 확인**

기대: Console에 에러 0건.

- [ ] **Step 5: Play 검증 — 동작 불변**

Main Scene 단독 Play. 이 태스크는 동작이 바뀌면 안 된다.
기대: 수배 리스트가 이전과 똑같이 뜬다. Console 로그가 `용의자 N명 중 공개 N명`(두 수가 같음)으로 나온다.

- [ ] **Step 6: 커밋**

```bash
git add Assets/Scripts/NPC/AppearanceAssigner.cs && git commit -m "제보 전화 — 몽타주 발행을 공개분으로 게이트 + RevealMontage (#102)"
```

---

## Task 3: 예비 용의자 풀 확정과 공개 분리

`m_criminalCount`의 의미가 "이번 라운드 진범 수" → "예비 풀 크기 = 최대 수배 수"로 바뀐다 (설계 §6). 이름·툴팁·로그를 함께 고쳐야 오해가 없다.

**⚠ `[FormerlySerializedAs]`를 반드시 붙일 것.** `SerializeField` 이름을 그냥 바꾸면 씬에 저장된 기존 값이 유실되고 코드 기본값으로 리셋된다.

**Files:**
- Modify: `Assets/Scripts/NPC/CriminalAssigner.cs`

**Interfaces:**
- Consumes: `CitizenIdentity.AssignProfile`(기존)
- Produces: 관측 가능한 변화 — `CriminalNpcs`에 예비 용의자가 포함되고, 그중 앞 `m_initialRevealCount`명만 `IsCriminal = true`.

- [ ] **Step 1: using 추가**

파일 맨 위 `using UnityEngine;` **아래**에 추가한다:

```csharp
using UnityEngine.Serialization;
```

- [ ] **Step 2: 인스펙터 필드 교체**

`[Header("진범 수 (#127)")]`부터 `private int m_criminalCount = 1;`까지의 블록 전체를 다음으로 교체한다:

```csharp
    [Header("수배 용의자 (#127 · #102)")]
    [Tooltip("이번 라운드의 예비 용의자 풀 크기 = 최대 수배 수. 라운드 시작에 전원 확정되지만 공개는 나눠서 된다(제보 전화). NPC 수보다 크면 NPC 수로 잘라 배정한다(경고 로그)")]
    [Min(1)]
    [FormerlySerializedAs("m_criminalCount")] // 씬에 저장된 기존 값 보존 — 의미가 '진범 수'에서 '예비 풀 크기'로 바뀌었다 (#102)
    [SerializeField]
    private int m_maxWantedCount = 3;

    [Tooltip("라운드 시작에 이미 수배로 공개된 용의자 수. 나머지는 미공개로 대기하다가 제보 전화를 받을 때마다 1명씩 공개된다 (#102). 0이면 수배 없이 시작한다")]
    [Min(0)]
    [SerializeField]
    private int m_initialRevealCount = 1;
```

- [ ] **Step 3: AssignAll의 선정·클램프 블록 교체**

`AssignAll` 안의 다음 블록을

```csharp
        // 진범 수는 NPC 수를 넘을 수 없다 — 초과 지정 시 잘라서 배정한다 (#127)
        int criminalCount = Mathf.Clamp(m_criminalCount, 1, npcs.Count);
        if (m_criminalCount > npcs.Count)
            Debug.LogWarning(
                $"CriminalAssigner: 진범 수({m_criminalCount})가 NPC 수({npcs.Count})보다 많아 {criminalCount}명으로 잘라 배정한다",
                this
            );

        HashSet<int> criminalIndices = PickCriminalIndices(npcs.Count, criminalCount);
```

다음으로 교체한다:

```csharp
        // 예비 풀 크기는 NPC 수를 넘을 수 없다 — 초과 지정 시 잘라서 배정한다 (#127)
        int suspectCount = Mathf.Clamp(m_maxWantedCount, 1, npcs.Count);
        if (m_maxWantedCount > npcs.Count)
            Debug.LogWarning(
                $"CriminalAssigner: 최대 수배 수({m_maxWantedCount})가 NPC 수({npcs.Count})보다 많아 {suspectCount}명으로 잘라 배정한다",
                this
            );

        // 최초 공개 수는 풀 크기를 넘을 수 없다 — 넘으면 전원 공개(= 제보 전화가 아무것도 안 하는 구성)
        int revealCount = Mathf.Clamp(m_initialRevealCount, 0, suspectCount);

        HashSet<int> criminalIndices = PickCriminalIndices(npcs.Count, suspectCount);
```

- [ ] **Step 4: 로그 헤더 갱신**

같은 메서드의

```csharp
        logBuilder.AppendLine($"시민 프로필 배정 완료 ({npcs.Count}명, 진범 {criminalCount}명):");
```

을 교체한다:

```csharp
        logBuilder.AppendLine($"시민 프로필 배정 완료 ({npcs.Count}명, 예비 용의자 {suspectCount}명 중 {revealCount}명 공개):");
```

- [ ] **Step 5: 루프 안 배정 분기 교체**

루프 안의 다음 블록을

```csharp
            bool isCriminal = criminalIndices.Contains(i);
            identity.AssignProfile(profile, isCriminal);
```

다음으로 교체한다:

```csharp
            // 예비 풀은 라운드 시작에 전부 확정하고 공개만 나눈다 — 전화 시점에 몽타주를 역생성하면
            // 부합 인원 수를 통제할 수 없어 디코이 설계가 깨진다 (#102 설계 결정 1)
            bool isSuspect = criminalIndices.Contains(i);
            // m_criminalNpcs에 담기기 전에 세므로 이 비교가 곧 '앞 revealCount명'이다
            bool isCriminal = isSuspect && m_criminalNpcs.Count < revealCount;
            identity.AssignProfile(profile, isCriminal);
```

- [ ] **Step 6: 목록 적재를 예비 풀 전원으로 확대**

같은 루프 안의

```csharp
            if (isCriminal)
            {
                m_criminalNpcs.Add(npcs[i]);
                m_wantedProfiles.Add(profile);
            }
```

을 교체한다:

```csharp
            // 미공개 예비 용의자도 목록에 담는다 — AppearanceAssigner가 이 목록으로 디코이와 몽타주를
            // 만들고, 승격(PromoteNext)도 여기서 다음 대상을 찾는다. 공개 여부는 IsCriminal이 가른다 (#102)
            if (isSuspect)
            {
                m_criminalNpcs.Add(npcs[i]);
                m_wantedProfiles.Add(profile);
            }
```

- [ ] **Step 7: 역할 로그 태그 갱신**

```csharp
            string roleTag = isCriminal ? $"  ← 범인 ({reaction})"
                : reaction != ReactionType.Compliant ? $"  (미끼: {reaction})"
                : "";
```

을 교체한다:

```csharp
            string roleTag = isCriminal ? $"  ← 수배 공개 ({reaction})"
                : isSuspect ? $"  ← 예비 용의자 · 미공개 ({reaction})"
                : reaction != ReactionType.Compliant ? $"  (미끼: {reaction})"
                : "";
```

- [ ] **Step 8: 프로퍼티·클래스 주석 갱신**

`CriminalNpcs` 주석을 교체한다. 기존:

```csharp
    /// <summary>실제 범인으로 지정된 NPC들. 배정 전에는 비어 있다. (#127)</summary>
```

교체 후:

```csharp
    /// <summary>
    /// 이번 라운드의 예비 용의자 전원 — 공개된 수배와 미공개 대기분을 모두 포함한다. 배정 전에는 비어 있다. (#127 · #102)
    /// 공개 여부는 각 NPC의 <see cref="CitizenIdentity.IsCriminal"/>이 가른다 — 이 목록에 있다고 진범인 것은 아니다.
    /// </summary>
```

`WantedProfiles` 주석의 `범인들의 프로필`을 `예비 용의자 전원의 프로필`로 바꾼다.

클래스 XML 주석의

```csharp
/// 스폰 완료 후 모든 NPC에 랜덤 시민 프로필을 채우고, 인스펙터에서 지정한 수만큼 실제 범인으로 지정한다. (이슈 #38/#127)
```

를 교체한다:

```csharp
/// 스폰 완료 후 모든 NPC에 랜덤 시민 프로필을 채우고, 인스펙터에서 지정한 수만큼 예비 용의자로 지정한다. (이슈 #38/#127)
/// 예비 용의자는 라운드 시작에 전원 확정되지만 앞 N명만 수배로 공개되고, 나머지는 제보 전화로 1명씩 공개된다 (#102).
```

- [ ] **Step 9: 컴파일 확인**

기대: Console에 에러 0건.

- [ ] **Step 10: Play 검증 — 여기서 처음 눈에 보인다**

Main Scene 단독 Play. 인스펙터에서 `Max Wanted Count = 3`, `Initial Reveal Count = 1` 확인.

기대:
1. 배정 로그에 `예비 용의자 3명 중 1명 공개`, 태그가 `← 수배 공개` 1건 + `← 예비 용의자 · 미공개` 2건.
2. 본부 수배 리스트에 **1건만** 뜬다. HUD 분모가 `0/1`.
3. 미공개 용의자를 검거·인계하면 **오검거** 판정(보상 0). 수배 리스트는 그대로.

**⚠ 씬 값 확인:** `[FormerlySerializedAs]`로 기존 값이 넘어왔는지 인스펙터에서 확인할 것. `Max Wanted Count`가 이전 `진범 수` 값(예: 1)로 보인다면 정상 이관된 것이다 — 테스트하려면 3으로 올린다.

- [ ] **Step 11: 커밋**

```bash
git add Assets/Scripts/NPC/CriminalAssigner.cs && git commit -m "제보 전화 — 예비 용의자 풀 확정과 공개 분리 (#102)"
```

---

## Task 4: PromoteNext와 승격 가드

승격 로직 본체. 아직 부르는 곳이 없으므로 **동작이 바뀌지 않는다** — Task 5에서 전화기가 연결한다.

가드가 없으면 아무도 못 잡는 유령 항목이 수배 리스트에 남는다 (설계 §3).

**Files:**
- Modify: `Assets/Scripts/NPC/CriminalAssigner.cs`

**Interfaces:**
- Consumes: `CitizenIdentity.SetCriminal`(Task 1), `AppearanceAssigner.RevealMontage`(Task 2), `NpcController.IsDelivered`, `PlayerEscorter.FindEscorterOf`
- Produces:
  - `CriminalAssigner.HasPendingSuspect` → `bool`
  - `CriminalAssigner.PromoteNext()` → `bool`

- [ ] **Step 1: 승격 API 추가**

`AssignAll` 메서드 **바로 아래**, `PickCriminalIndices`의 XML 주석 **바로 위**에 삽입한다:

```csharp

    // ---- 제보 전화 승격 (#102) ----
    //
    // 배정 자체가 서버(또는 오프라인)에서만 일어나므로 승격도 같은 쪽에서만 의미가 있다.
    // 호출자(TipCallPhone)가 서버 권위를 게이트한다 — 여기서 다시 막지 않는다.

    /// <summary>
    /// 아직 공개되지 않은 예비 용의자가 남아 있는가 — 전화를 계속 걸지의 기준 (#102 설계 결정 5).
    /// 연행·끌기 중인 대상도 '남아 있다'로 센다: 곧 판정되면 IsDelivered로 자동으로 빠지고,
    /// 석방되면 다시 승격 후보가 된다. 여기서 빼면 마지막 예비 용의자를 끌고 가는 동안 울린
    /// 전화 하나 때문에 그 라운드 전화가 영영 끊긴다.
    /// </summary>
    public bool HasPendingSuspect
    {
        get
        {
            for (int i = 0; i < m_criminalNpcs.Count; i++)
                if (IsPending(m_criminalNpcs[i]))
                    return true;
            return false;
        }
    }

    /// <summary>
    /// 대기 중인 예비 용의자 1명을 수배로 공개한다 — 제보 전화를 받았을 때 호출한다. (#102)
    /// 성공하면 true. 지금 승격 가능한 대상이 없으면 false — 그 전화 한 번을 놓친 것일 뿐이므로
    /// 호출자는 다음 수신을 그대로 예약하면 된다 (풀 소진 판정은 HasPendingSuspect로 따로 한다).
    /// </summary>
    public bool PromoteNext()
    {
        AppearanceAssigner appearance = App.Game.Appearance;
        if (appearance == null)
        {
            // 몽타주를 발행하지 못하면 수배 리스트에 뜨지 않는다 — IsCriminal만 켜면
            // 아무도 모르는 진범이 생기므로, 켜기 전에 막는다
            Debug.LogWarning("CriminalAssigner: AppearanceAssigner를 찾지 못해 승격할 수 없다", this);
            return false;
        }

        NpcController npc = FindNextPromotable();
        if (npc == null)
            return false;

        CitizenIdentity identity = npc.GetComponent<CitizenIdentity>();
        identity.SetCriminal(true);

        // 예비 용의자는 시민 가중치(대부분 순응)로 뽑혀 있다 — 범인 가중치로 다시 뽑는다.
        // Reaction은 서버 전용이라 바꿔도 플레이어에게 티가 나지 않는다 (#102 설계 결정 6)
        identity.AssignReaction(RollReaction(m_compliantWeight, m_fleeWeight, m_resistWeight));

        // 라운드 시작에 보관해 둔 몽타주를 그대로 발행한다 — WantedListManager가 이 이벤트로
        // NetworkList 추가와 TotalWanted++ 를 한다(기존 경로 재사용)
        appearance.RevealMontage(npc);

        CitizenProfile profile = identity.Profile;
        Debug.Log($"[제보 전화] 수배 공개: {(profile != null ? profile.CitizenName : npc.name)} ({identity.Reaction})");
        return true;
    }

    /// <summary>미공개 예비 용의자인가 — 살아 있고, 아직 공개 전이고, 판정이 끝나지 않았다. (#102)</summary>
    private static bool IsPending(NpcController npc)
    {
        // 디스폰·파괴된 대상은 Unity null로 잡힌다 — IsSpawned는 오프라인에서 항상 false라 쓸 수 없다
        if (npc == null)
            return false;

        CitizenIdentity identity = npc.GetComponent<CitizenIdentity>();
        if (identity == null || identity.IsCriminal)
            return false;

        // 미공개 상태로 오검거되어 판정이 끝난 대상 — 그 몽타주를 등록하면 잡을 대상이 없다 (#230)
        return !npc.IsDelivered;
    }

    /// <summary>지금 당장 승격시킬 수 있는 첫 후보. 없으면 null. (#102 설계 §3 가드)</summary>
    private NpcController FindNextPromotable()
    {
        for (int i = 0; i < m_criminalNpcs.Count; i++)
        {
            NpcController npc = m_criminalNpcs[i];
            if (!IsPending(npc))
                continue;

            // 연행·끌기 중 — 곧 판정될 대상이라 등록 직후 사라진다. 건너뛰되 풀 소진으로는 세지 않는다
            // (FindEscorterOf는 연행과 밧줄 끌기를 둘 다 본다, #269)
            if (PlayerEscorter.FindEscorterOf(npc) != null)
                continue;

            return npc;
        }
        return null;
    }
```

- [ ] **Step 2: 컴파일 확인**

기대: Console에 에러 0건.

- [ ] **Step 3: 커밋**

```bash
git add Assets/Scripts/NPC/CriminalAssigner.cs && git commit -m "제보 전화 — PromoteNext와 승격 가드 (#102)"
```

---

## Task 5: TipCallPhone

본부에 놓이는 전화기. 수신 타이머와 승격은 서버에서만 돌고, 울림 여부만 동기화한다.

수신 스케줄은 `SuddenEventManager`의 라운드 페이즈 폴링 + `Time.time` 예약 방식을 그대로 따른다 — 같은 종류의 주기 발생이라 다른 관용구를 만들 이유가 없다.

**Files:**
- Create: `Assets/Scripts/HQ/TipCallPhone.cs`

**Interfaces:**
- Consumes: `CriminalAssigner.PromoteNext()`·`HasPendingSuspect`(Task 4), `RoundManager.Phase`, `IInteractable`
- Produces: `TipCallPhone.IsRinging` → `bool`, `TipCallPhone.OnRingingChanged` → `event Action` (벨소리·표시 연출이 나중에 구독할 훅)

- [ ] **Step 1: TipCallPhone.cs 생성**

```csharp
using System;
using Unity.Netcode;
using UnityEngine;

/// <summary>
/// 본부 제보 전화 (#102, GDD 4-2) — 간헐적으로 울리고, 받으면 대기 중이던 예비 용의자 1명이
/// 수배 리스트에 공개된다. 전화 내용은 없다: 받는 행위 자체가 기능이다.
/// 이게 "본부에 최소 1명 상주"의 이유이자 "전화 오기 전까지만 현장 다녀오자"는 도박을 만든다.
///
/// 서버 권위 — 수신 타이머·승격은 서버(또는 오프라인)에서만 돌고, 울림 여부만 동기화한다.
/// 벨소리·표시는 각 클라의 로컬 연출이므로 OnRingingChanged를 구독해 붙이면 된다(이 이슈 범위 밖).
/// 씬 배치·모델·콜라이더는 Editor 작업이다 — 코드는 상호작용 경로까지만 만든다 (설계 §5).
/// </summary>
[RequireComponent(typeof(NetworkObject))]
public class TipCallPhone : NetworkBehaviour, IInteractable
{
    private CriminalAssigner Assigner => App.Game.CriminalAssigner;
    private RoundManager Round => App.Game.Round;

    [Header("수신 간격 (#102)")]
    [Tooltip("전화가 끊긴 뒤 다음 수신까지의 최소 대기(초). 최대값과의 사이에서 불규칙하게 뽑는다")]
    [Min(0f)]
    [SerializeField] private float m_minInterval = 40f;

    [Tooltip("다음 수신까지의 최대 대기(초)")]
    [Min(0f)]
    [SerializeField] private float m_maxInterval = 90f;

    [Tooltip("라운드 시작 후 첫 전화까지의 추가 유예(초) — 시작하자마자 울리지 않게")]
    [Min(0f)]
    [SerializeField] private float m_startDelay = 20f;

    [Header("울림")]
    [Tooltip("한 번 울리고 스스로 끊기까지의 시간(초). 이게 곧 '현장 다녀올 수 있는 시간'이라 체감에 직결된다")]
    [Min(1f)]
    [SerializeField] private float m_ringDuration = 15f;

    // 울림 여부만 동기화한다 — 타이머와 승격은 서버 안에서 끝난다
    private readonly NetworkVariable<bool> m_isRingingSynced = new(false);

    // 서버·오프라인의 진실값 (NpcController.m_networkState와 동일 이중 구조)
    private bool m_isRinging;

    private float m_nextRingTime;
    private float m_ringEndTime;
    private bool m_scheduling;
    private RoundPhase m_lastPhase = RoundPhase.Preparing;

    /// <summary>지금 울리는 중인가. 서버·오프라인은 진실값, 원격 피어는 동기화 값.</summary>
    public bool IsRinging => IsSpawned && !IsServer ? m_isRingingSynced.Value : m_isRinging;

    /// <summary>울림 시작·종료 — 벨소리와 표시 연출이 구독할 훅. 전 피어에서 발행된다.</summary>
    public event Action OnRingingChanged;

    // 스폰 전(오프라인 단독 Play)이면 이 피어가 곧 권위다 — SuddenEventManager와 동일
    private bool IsAuthority => !IsSpawned || IsServer;

    public override void OnNetworkSpawn()
    {
        m_isRingingSynced.OnValueChanged += HandleRingingSyncedChanged;
    }

    public override void OnNetworkDespawn()
    {
        m_isRingingSynced.OnValueChanged -= HandleRingingSyncedChanged;
    }

    private void HandleRingingSyncedChanged(bool previous, bool current) => OnRingingChanged?.Invoke();

    // ---- 상호작용 (#184) ----

    public void Interact(GameObject interactor)
    {
        if (!CanInteract(interactor))
            return;

        // 세션 밖(오프라인 단독 Play)에서는 RPC를 보낼 곳이 없다 — 이 피어가 곧 서버다
        if (!IsSpawned)
        {
            Answer();
            return;
        }

        RequestAnswerRpc();
    }

    /// <summary>울리는 중에만 받을 수 있다 — 조준 윤곽선도 그때만 켜진다. (#184)</summary>
    public bool CanInteract(GameObject interactor) => IsRinging;

    [Rpc(SendTo.Server)]
    private void RequestAnswerRpc()
    {
        // CanInteract는 조준 피드백용 클라이언트 게이팅이라 RPC 직접 호출을 막지 못한다.
        // 서버에서 한 번 더 검증한다 — CCTVSwitcher.RequestTogglePowerRpc와 같은 이유 (#362)
        if (!m_isRinging)
            return;

        Answer();
    }

    // ---- 수신 스케줄 (서버 · 오프라인 전용) ----

    private void Update()
    {
        if (!IsAuthority)
            return;
        if (Round == null)
            return;

        // 라운드 페이즈 전이 감지 — InProgress 진입 시 수신 시작, 이탈 시 울림을 끊고 멈춘다
        RoundPhase phase = Round.Phase;
        if (phase != m_lastPhase)
        {
            HandlePhaseChanged(phase);
            m_lastPhase = phase;
        }

        if (!m_scheduling)
            return;

        if (m_isRinging)
        {
            // 울림 시간 초과 = 놓침. 승격 없이 다음 수신만 예약한다 — 기회는 복구되지 않는다
            if (Time.time >= m_ringEndTime)
            {
                SetRinging(false);
                ScheduleNext();
                Debug.Log("[제보 전화] 받지 않아 끊겼다 — 수배 공개 기회를 놓쳤다");
            }
            return;
        }

        if (Time.time < m_nextRingTime)
            return;

        // 예비 풀이 비면 전화를 멈춘다 — 받아도 아무 일 없는 전화는 플레이어를 속이는 셈이고,
        // 본부 상주 압박도 그 시점엔 풀어주는 게 맞다 (#102 설계 결정 5)
        if (Assigner == null || !Assigner.HasPendingSuspect)
        {
            m_scheduling = false;
            Debug.Log("[제보 전화] 예비 용의자가 모두 공개되어 더 이상 전화가 오지 않는다");
            return;
        }

        SetRinging(true);
        m_ringEndTime = Time.time + m_ringDuration;
        Debug.Log($"[제보 전화] 수신 — {m_ringDuration:0}초 안에 받아야 한다");
    }

    private void HandlePhaseChanged(RoundPhase phase)
    {
        if (phase == RoundPhase.InProgress)
        {
            m_scheduling = true;
            ScheduleNext(m_startDelay);
        }
        else
        {
            // 라운드가 끝났거나 준비 상태로 되돌아감 — 울리던 전화를 끊고 수신을 멈춘다
            m_scheduling = false;
            SetRinging(false);
        }
    }

    // 전화를 받았다 — 서버(또는 오프라인)에서만 돈다
    private void Answer()
    {
        SetRinging(false);

        if (Assigner == null)
        {
            Debug.LogWarning("TipCallPhone: CriminalAssigner를 찾지 못해 수배를 공개할 수 없다", this);
        }
        else if (!Assigner.PromoteNext())
        {
            // 지금 승격 가능한 후보가 없다(연행 중 등). 풀 소진 판정은 HasPendingSuspect가 따로 하므로
            // 여기서 멈추지 않는다 — 다음 전화 때 다시 시도한다
            Debug.Log("[제보 전화] 받았지만 지금 공개할 수 있는 용의자가 없다 — 다음 전화를 기다린다");
        }

        ScheduleNext();
    }

    private void ScheduleNext(float extraDelay = 0f)
    {
        m_nextRingTime = Time.time + extraDelay + UnityEngine.Random.Range(m_minInterval, m_maxInterval);
    }

    private void SetRinging(bool value)
    {
        if (m_isRinging == value)
            return;

        m_isRinging = value;

        // 호스트는 동기화 변수 쓰기가 곧 자기 이벤트 발행이 아니다 — 아래에서 직접 발행한다
        if (IsSpawned && IsServer)
            m_isRingingSynced.Value = value;

        OnRingingChanged?.Invoke();
    }
}
```

- [ ] **Step 2: 컴파일 확인**

기대: Console에 에러 0건.

- [ ] **Step 3: 씬 배치 (Editor 작업)**

Main Scene 본부 안에:
1. 전화기 오브젝트를 만든다(임시로 Cube도 무방 — 모델은 후속).
2. `NetworkObject` + `TipCallPhone` 부착. 상호작용 레이캐스트에 걸리도록 Collider와 레이어를 다른 `IInteractable`(예: CCTV 버튼)과 같은 기준으로 맞춘다.
3. 씬 `NetworkObject`이므로 `DefaultNetworkPrefabs.asset` 등록은 불필요하다 — 프리팹으로 뽑아 런타임 스폰할 계획이면 그때 등록한다.
4. 테스트를 빠르게 하려면 `Min Interval = 10`, `Max Interval = 15`, `Start Delay = 3`으로 임시 조정한다. **커밋 전 기본값으로 되돌릴 것.**

- [ ] **Step 4: Play 검증 (단독)**

설계 §7의 1~8·10번을 수행한다:

1. 라운드 시작 → 수배 리스트에 **1건만**.
2. 미공개 용의자 검거·인계 → **오검거**(보상 0), 수배 리스트 변화 없음.
3. 전화 수신 → E로 받기 → 수배 리스트 **1건 추가**, HUD 분모 +1.
4. 추가된 용의자 검거·인계 → **현상수배범** 판정(보상 10000).
5. 3번 이후에도 **기존 몽타주로 1번 용의자를 계속 찾을 수 있는지**(공개 축 불변).
6. 전화 방치 → `받지 않아 끊겼다` 로그, 수배 그대로, 다음 전화가 옴.
7. 미공개 용의자를 오검거로 없앤 뒤 전화 받기 → 그 대상을 건너뛰고 다음 후보가 승격.
8. 풀 소진 후 → `더 이상 전화가 오지 않는다` 로그, 이후 무음.
10. 울리지 않을 때 E → 무반응, 윤곽선 없음.

- [ ] **Step 5: Play 검증 (MPPM 2인)**

설계 §7의 9번:
- 울림 표시(로그/연출 훅)가 양쪽에 보이고, **클라이언트 쪽에서 받아도** 양쪽 수배 리스트가 같이 갱신되는지.
- 클라 콘솔은 `Library/VP/mppm*/Logs/Editor.log`에서 확인한다.

- [ ] **Step 6: 커밋**

인스펙터 임시값을 기본값으로 되돌린 뒤:

```bash
git add Assets/Scripts/HQ/TipCallPhone.cs Assets/Scripts/HQ/TipCallPhone.cs.meta && git commit -m "제보 전화 — 본부 전화기 수신·응답 (#102)"
```

씬 변경은 별도 커밋으로 남긴다:

```bash
git add "Assets/Scenes/Main Scene.unity" && git commit -m "제보 전화 — 본부에 전화기 배치 (#102)"
```

---

## Task 6: 테스트 라벨 구분과 GDD 갱신

`TestCriminalLabel`이 `CriminalNpcs` 전원에 "범인" 라벨을 붙이므로, 미공개 예비 용의자까지 범인으로 보인다. 테스트 2·3·7을 판별하려면 공개/미공개가 눈에 보여야 한다.

GDD 6장 178줄의 제보 전화 갱신 방식은 아직 `(제안)` / `(대안: 신규 스폰 — 결정 필요)`로 남아 있다. 이 이슈가 "이미 맵에 있던 NPC를 수배 등록"으로 확정했으니 반영한다.

**Files:**
- Modify: `Assets/Scripts/Test/TestCriminalLabel.cs`
- Modify: `docs/GDD.md`

- [ ] **Step 1: 라벨을 공개 상태에 따라 갱신**

`TestCriminalLabel.cs`의 `m_text`·`m_color` 필드 선언을 교체한다:

```csharp
    [SerializeField] private string m_text = "수배";
    [SerializeField] private Color m_color = Color.red;

    [Header("미공개 예비 용의자 (#102)")]
    [Tooltip("제보 전화로 아직 공개되지 않은 대기 중 용의자에게 붙는 라벨")]
    [SerializeField] private string m_pendingText = "예비(미공개)";
    [SerializeField] private Color m_pendingColor = Color.gray;
```

`HandleCriminalAssigned`를 교체한다:

```csharp
    private void HandleCriminalAssigned(IReadOnlyList<NpcController> criminals)
    {
        foreach (NpcController criminal in criminals)
        {
            if (criminal == null)
                continue;

            // 이미 붙어 있으면 중복 부착 방지 (Start 보정과 이벤트가 겹칠 수 있음)
            if (criminal.GetComponent<TestWorldLabel>() != null)
                continue;

            TestWorldLabel label = criminal.gameObject.AddComponent<TestWorldLabel>();
            Apply(label, criminal);
        }
    }

    // 제보 전화 승격(#102)은 배정 이벤트 밖에서 일어나므로, 라벨은 폴링으로 따라간다.
    // 테스트 전용 스크립트이고 대상이 예비 풀 크기(보통 3명)뿐이라 비용을 따질 규모가 아니다.
    private void Update()
    {
        if (Assigner == null)
            return;

        IReadOnlyList<NpcController> criminals = Assigner.CriminalNpcs;
        for (int i = 0; i < criminals.Count; i++)
        {
            if (criminals[i] == null)
                continue;

            TestWorldLabel label = criminals[i].GetComponent<TestWorldLabel>();
            if (label != null)
                Apply(label, criminals[i]);
        }
    }

    // 공개된 수배인지 대기 중인 예비 용의자인지로 문구·색을 가른다 (#102)
    private void Apply(TestWorldLabel label, NpcController npc)
    {
        CitizenIdentity identity = npc.GetComponent<CitizenIdentity>();
        bool revealed = identity != null && identity.IsCriminal;
        label.Configure(revealed ? m_text : m_pendingText, revealed ? m_color : m_pendingColor);
    }
```

클래스 XML 주석 첫 줄을 교체한다:

```csharp
/// [테스트용] 예비 용의자 머리 위에 라벨을 띄운다 — 공개된 수배와 미공개 대기분을 구분한다. (#127 · #102)
```

(`TestWorldLabel.Configure`는 이미 만들어진 `TextMesh`의 `text`·`color`만 대입한다 — 매 프레임 재호출해도 할당이 없다. 별도 캐시가 필요 없다.)

- [ ] **Step 2: GDD 6장 갱신**

`docs/GDD.md` 178줄을 교체한다. 기존:

```markdown
- **제보 전화 갱신 방식 (제안)**: 명단에 추가되는 용의자는 신규 스폰이 아니라 **이미 맵에 있던 NPC가 수배 등록**됨 → "아까 지나친 그 놈"을 다시 찾는 재미. (대안: 신규 스폰 — 결정 필요)
```

교체 후:

```markdown
- **제보 전화 갱신 방식 (2026-07-27 확정, #102)**: 명단에 추가되는 용의자는 신규 스폰이 아니라 **이미 맵에 있던 NPC가 수배 등록**된다 → "아까 지나친 그 놈"을 다시 찾는 재미. 라운드 시작에 예비 용의자 풀 전체(외형·디코이·몽타주 포함)를 확정해 두고 공개만 나눈다 — 전화 시점에 몽타주를 역생성하면 부합 인원 수를 통제할 수 없어 디코이 설계가 깨진다. 미공개 용의자를 잡으면 오검거다. 수배 HUD 분모는 공개될 때마다 늘어난다(`0/1` → 전화 → `1/2`). 예비 풀이 소진되면 전화가 오지 않는다.
```

- [ ] **Step 3: 컴파일 확인 + Play 검증**

Main Scene 단독 Play.
기대: 공개 수배 1명 머리 위 빨간 `수배`, 나머지 예비 2명은 회색 `예비(미공개)`. 전화를 받으면 그중 1명이 빨간 `수배`로 바뀐다.

- [ ] **Step 4: 커밋**

```bash
git add Assets/Scripts/Test/TestCriminalLabel.cs docs/GDD.md && git commit -m "제보 전화 — 테스트 라벨 공개/미공개 구분 + GDD 갱신 (#102)"
```

---

## 이 플랜이 끝나도 남는 것

- **벨소리·울림 표시 연출** — 설계 §5대로 범위 밖. `OnRingingChanged`와 `IsRinging`이 훅으로 남아 있으니 나중에 붙이면 된다. 본부에서 소리로 인지할 수단이 없으면 실제 플레이 감각은 확인할 수 없다.
- **전화기 모델·최종 배치** — 임시 오브젝트로 두고 아트 반영은 후속.
- **최초 공개 수 0 구성** — 설계 §6의 미해결. HUD 분모가 0으로 시작할 때 표시가 깨지는지 확인이 필요하다. Task 5 Play 검증에 넣지 않았다(기본 구성이 아니다).
- **라운드 할당량과의 값 연동** — 예비 풀 크기 = 최대 수배 수라 할당량 이슈와 맞물린다. 여기서는 인스펙터 값으로 두고, 할당량 쪽이 확정되면 그쪽이 주도한다.
